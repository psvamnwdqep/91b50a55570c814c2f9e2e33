name: Build

on:
  push:
    branches:
      - dev

jobs:
  build:
    name: build
    runs-on: ubuntu-24.04
    steps:
      - name: checkout
        uses: actions/checkout@v4
      - name: build
        run: sudo build/build.sh

